{{ render '@header' }}
<div class="c-layout3col">
  <aside>
    {{ render '@secondarynav' }}
  </aside>
  <main>
    {{ render '@breadcrumb' }}
    {{> @heading--1 }}
    {{#each paragraph}}
      <p>{{ program.text }}</p>
    {{/each}}
    {{ render '@mainbox--1' }}
    {{ render '@mainbox--2' }}
    {{ render '@mainbox--2' }}
    {{#each paragraph}}
      <p>{{ program.text }}</p>
    {{/each}}
    {{ render '@halfboxes' }}
    {{ render '@staff' }}
    {{ render '@updated' }}
  </main>
  <aside>
    {{ render '@news' }}
    <div class="c-layout__sidebar-items">
      {{ render '@sidebarbox--2' }}
      {{ render '@sidebarbox--2' }}
      {{ render '@sidebarbox--1' }}
    </div>
  </aside>
</div>
{{ render '@footer' }}
