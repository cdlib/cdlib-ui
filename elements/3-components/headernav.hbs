{{!-- Read notes in component config --}}
<nav aria-label="global" class="c-headernav c-mobilenav">
  <ul>
    {{#each list1}}
    <li class="{{ columnClass }}">{{> @link }}
      <ul>
        {{#each ../list2}}
        <li>{{> @link }}
          <ul>
            {{#each ../../list3}}
            <li>{{> @link }}</li>
            {{/each}}
          </ul>
        </li>
        {{/each}}
      </ul>
    </li>
    {{/each}}
  </ul>
</nav>
