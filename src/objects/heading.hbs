<h{{ heading.level }} {{#if heading.id }}id="{{ heading.id }}"{{/if}}>
  {{#if heading.link }}
    {{> link heading }}
  {{else}}
    {{{ heading.text }}} {{#if heading.abbr }}<abbr title="{{ heading.abbr.title }}">{{ heading.abbr.text }}</abbr>{{/if}}
  {{/if}}
</h{{ heading.level }}>
