<section class="c-quicklink1 c-sidebarnav">
  <header>
    {{> heading quicklink1 }}
  </header>
  <ul>
  {{#each quicklink1.menu}}
    <li>{{> link }}</li>
  {{/each}}
  </ul>
</section>

<section class="c-quicklink2 c-sidebarnav">
  <header>
    {{> heading quicklink2 }}
  </header>
  <ul>
  {{#each quicklink2.menu}}
    <li>{{> link }}</li>
  {{/each}}
  </ul>
</section>
